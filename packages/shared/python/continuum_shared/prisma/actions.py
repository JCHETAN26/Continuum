# -*- coding: utf-8 -*-
# code generated by Prisma. DO NOT EDIT.
# pyright: reportUnusedImport=false
# fmt: off
from __future__ import annotations

# global imports for type checking
from builtins import bool as _bool
from builtins import int as _int
from builtins import float as _float
from builtins import str as _str
import sys
import decimal
import datetime
from typing import (
    TYPE_CHECKING,
    Optional,
    Iterable,
    Iterator,
    Sequence,
    Callable,
    ClassVar,
    NoReturn,
    TypeVar,
    Generic,
    Mapping,
    Tuple,
    Union,
    List,
    Dict,
    Type,
    Any,
    Set,
    overload,
    cast,
)
from typing_extensions import TypedDict, Literal


LiteralString = str
# -- template actions.py.jinja --
from typing import TypeVar
import warnings

from . import types, errors, bases
from ._compat import model_parse
from ._constants import CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED

if TYPE_CHECKING:
    from .client import Prisma
    from .bases import _PrismaModel


_PrismaModelT = TypeVar('_PrismaModelT', bound='_PrismaModel')


class DocumentActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Document]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Document.prisma().query_raw(
            'SELECT * FROM Document WHERE id = $1',
            'bbadfchfja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Document
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Document.prisma().query_first(
            'SELECT * FROM Document WHERE externalId = $1',
            'dhheabfhf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.DocumentCreateInput,
        include: Optional[types.DocumentInclude] = None
    ) -> _PrismaModelT:
        """Create a new Document record.

        Parameters
        ----------
        data
            Document record data
        include
            Specifies which relations should be loaded on the returned Document model

        Returns
        -------
        prisma.models.Document
            The created Document record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Document record from just the required fields
        document = await Document.prisma().create(
            data={
                # data to create a Document record
                'externalId': 'ggciceaie',
                'idempotencyKey': 'bbehjachib',
                'text': 'cadfabfehe',
                'source': 'dgiiaaijj',
                'occurredAt': datetime.datetime.utcnow(),
                'contentHash': 'bfaiacjjfc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.DocumentCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Document records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Document record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Document.prisma().create_many(
            data=[
                {
                    # data to create a Document record
                    'externalId': 'eigcfgbif',
                    'idempotencyKey': 'bagcfbhiig',
                    'text': 'cghideieh',
                    'source': 'biabhbdai',
                    'occurredAt': datetime.datetime.utcnow(),
                    'contentHash': 'idghgaicb',
                },
                {
                    # data to create a Document record
                    'externalId': 'fjfddhigg',
                    'idempotencyKey': 'hjaecfifb',
                    'text': 'cbbbjbfcii',
                    'source': 'bbejhfidcb',
                    'occurredAt': datetime.datetime.utcnow(),
                    'contentHash': 'bgeecijdgg',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.DocumentWhereUniqueInput,
        include: Optional[types.DocumentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Document record.

        Parameters
        ----------
        where
            Document filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Document model

        Returns
        -------
        prisma.models.Document
            The deleted Document record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        document = await Document.prisma().delete(
            where={
                'id': 'bdiicjafbj',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.DocumentWhereUniqueInput,
        include: Optional[types.DocumentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Document record.

        Parameters
        ----------
        where
            Document filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Document model

        Returns
        -------
        prisma.models.Document
            The found Document record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        document = await Document.prisma().find_unique(
            where={
                'id': 'bgehebiafc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.DocumentWhereUniqueInput,
        include: Optional[types.DocumentInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Document record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Document filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Document model

        Returns
        -------
        prisma.models.Document
            The found Document record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        document = await Document.prisma().find_unique_or_raise(
            where={
                'id': 'bghffegacj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DocumentWhereInput] = None,
        cursor: Optional[types.DocumentWhereUniqueInput] = None,
        include: Optional[types.DocumentInclude] = None,
        order: Optional[Union[types.DocumentOrderByInput, List[types.DocumentOrderByInput]]] = None,
        distinct: Optional[List[types.DocumentScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Document records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Document records returned
        skip
            Ignore the first N results
        where
            Document filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Document model
        order
            Order the returned Document records by any field
        distinct
            Filter Document records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Document]
            The list of all Document records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Document records
        documents = await Document.prisma().find_many(take=10)

        # find the first 5 Document records ordered by the idempotencyKey field
        documents = await Document.prisma().find_many(
            take=5,
            order={
                'idempotencyKey': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DocumentWhereInput] = None,
        cursor: Optional[types.DocumentWhereUniqueInput] = None,
        include: Optional[types.DocumentInclude] = None,
        order: Optional[Union[types.DocumentOrderByInput, List[types.DocumentOrderByInput]]] = None,
        distinct: Optional[List[types.DocumentScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Document record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Document filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Document model
        order
            Order the returned Document records by any field
        distinct
            Filter Document records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Document
            The first Document record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Document record ordered by the text field
        document = await Document.prisma().find_first(
            skip=1,
            order={
                'text': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DocumentWhereInput] = None,
        cursor: Optional[types.DocumentWhereUniqueInput] = None,
        include: Optional[types.DocumentInclude] = None,
        order: Optional[Union[types.DocumentOrderByInput, List[types.DocumentOrderByInput]]] = None,
        distinct: Optional[List[types.DocumentScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Document record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Document filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Document model
        order
            Order the returned Document records by any field
        distinct
            Filter Document records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Document
            The first Document record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Document record ordered by the source field
        document = await Document.prisma().find_first_or_raise(
            skip=1,
            order={
                'source': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.DocumentUpdateInput,
        where: types.DocumentWhereUniqueInput,
        include: Optional[types.DocumentInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Document record.

        Parameters
        ----------
        data
            Document record data specifying what to update
        where
            Document filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Document model

        Returns
        -------
        prisma.models.Document
            The updated Document record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        document = await Document.prisma().update(
            where={
                'id': 'bhghchehcc',
            },
            data={
                # data to update the Document record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.DocumentWhereUniqueInput,
        data: types.DocumentUpsertInput,
        include: Optional[types.DocumentInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Document filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Document model

        Returns
        -------
        prisma.models.Document
            The created or updated Document record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        document = await Document.prisma().upsert(
            where={
                'id': 'dcgchcbbf',
            },
            data={
                'create': {
                    'id': 'dcgchcbbf',
                    'externalId': 'fjfddhigg',
                    'idempotencyKey': 'hjaecfifb',
                    'text': 'cbbbjbfcii',
                    'source': 'bbejhfidcb',
                    'occurredAt': datetime.datetime.utcnow(),
                    'contentHash': 'bgeecijdgg',
                },
                'update': {
                    'externalId': 'fjfddhigg',
                    'idempotencyKey': 'hjaecfifb',
                    'text': 'cbbbjbfcii',
                    'source': 'bbejhfidcb',
                    'occurredAt': datetime.datetime.utcnow(),
                    'contentHash': 'bgeecijdgg',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.DocumentUpdateManyMutationInput,
        where: types.DocumentWhereInput,
    ) -> int:
        """Update multiple Document records

        Parameters
        ----------
        data
            Document data to update the selected Document records to
        where
            Filter to select the Document records to update

        Returns
        -------
        int
            The total number of Document records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Document records
        total = await Document.prisma().update_many(
            data={
                'occurredAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DocumentWhereInput] = None,
        cursor: Optional[types.DocumentWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Document records present in the database

        Parameters
        ----------
        select
            Select the Document fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Document filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DocumentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Document.prisma().count()

        # results: prisma.types.DocumentCountAggregateOutput
        results = await Document.prisma().count(
            select={
                '_all': True,
                'metadata': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.DocumentCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DocumentWhereInput] = None,
        cursor: Optional[types.DocumentWhereUniqueInput] = None,
    ) -> types.DocumentCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.DocumentCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DocumentWhereInput] = None,
        cursor: Optional[types.DocumentWhereUniqueInput] = None,
    ) -> Union[int, types.DocumentCountAggregateOutput]:
        """Count the number of Document records present in the database

        Parameters
        ----------
        select
            Select the Document fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Document filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DocumentCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Document.prisma().count()

        # results: prisma.types.DocumentCountAggregateOutput
        results = await Document.prisma().count(
            select={
                '_all': True,
                'contentHash': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DocumentCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.DocumentWhereInput] = None
    ) -> int:
        """Delete multiple Document records.

        Parameters
        ----------
        where
            Optional Document filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Document records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Document records
        total = await Document.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.DocumentScalarFieldKeys'],
        *,
        where: Optional['types.DocumentWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DocumentAvgAggregateInput'] = None,
        sum: Optional['types.DocumentSumAggregateInput'] = None,
        min: Optional['types.DocumentMinAggregateInput'] = None,
        max: Optional['types.DocumentMaxAggregateInput'] = None,
        having: Optional['types.DocumentScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DocumentCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DocumentScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DocumentScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DocumentGroupByOutput']:
        """Group Document records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Document fields to group records by
        where
            Document filter to select records
        take
            Limit the maximum number of Document records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DocumentGroupByOutput]
            A list of dictionaries representing the Document record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Document records by objectKey values
        # and count how many records are in each group
        results = await Document.prisma().group_by(
            ['objectKey'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class EmbeddingActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.Embedding]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await Embedding.prisma().query_raw(
            'SELECT * FROM Embedding WHERE id = $1',
            'bdedcabahc',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.Embedding
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await Embedding.prisma().query_first(
            'SELECT * FROM Embedding WHERE documentId = $1',
            'ghfhiafcb',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.EmbeddingCreateInput,
        include: Optional[types.EmbeddingInclude] = None
    ) -> _PrismaModelT:
        """Create a new Embedding record.

        Parameters
        ----------
        data
            Embedding record data
        include
            Specifies which relations should be loaded on the returned Embedding model

        Returns
        -------
        prisma.models.Embedding
            The created Embedding record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a Embedding record from just the required fields
        embedding = await Embedding.prisma().create(
            data={
                # data to create a Embedding record
                'documentId': 'heejgedji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.EmbeddingCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple Embedding records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of Embedding record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await Embedding.prisma().create_many(
            data=[
                {
                    # data to create a Embedding record
                    'documentId': 'bjgjgibgbf',
                },
                {
                    # data to create a Embedding record
                    'documentId': 'bbbgbhfjge',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.EmbeddingWhereUniqueInput,
        include: Optional[types.EmbeddingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single Embedding record.

        Parameters
        ----------
        where
            Embedding filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned Embedding model

        Returns
        -------
        prisma.models.Embedding
            The deleted Embedding record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        embedding = await Embedding.prisma().delete(
            where={
                'id': 'igbehcbab',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.EmbeddingWhereUniqueInput,
        include: Optional[types.EmbeddingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique Embedding record.

        Parameters
        ----------
        where
            Embedding filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Embedding model

        Returns
        -------
        prisma.models.Embedding
            The found Embedding record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        embedding = await Embedding.prisma().find_unique(
            where={
                'id': 'bdadaadhag',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.EmbeddingWhereUniqueInput,
        include: Optional[types.EmbeddingInclude] = None
    ) -> _PrismaModelT:
        """Find a unique Embedding record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            Embedding filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned Embedding model

        Returns
        -------
        prisma.models.Embedding
            The found Embedding record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        embedding = await Embedding.prisma().find_unique_or_raise(
            where={
                'id': 'bgiggdidbf',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmbeddingWhereInput] = None,
        cursor: Optional[types.EmbeddingWhereUniqueInput] = None,
        include: Optional[types.EmbeddingInclude] = None,
        order: Optional[Union[types.EmbeddingOrderByInput, List[types.EmbeddingOrderByInput]]] = None,
        distinct: Optional[List[types.EmbeddingScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple Embedding records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of Embedding records returned
        skip
            Ignore the first N results
        where
            Embedding filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Embedding model
        order
            Order the returned Embedding records by any field
        distinct
            Filter Embedding records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.Embedding]
            The list of all Embedding records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 Embedding records
        embeddings = await Embedding.prisma().find_many(take=10)

        # find the first 5 Embedding records ordered by the modelVersionId field
        embeddings = await Embedding.prisma().find_many(
            take=5,
            order={
                'modelVersionId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EmbeddingWhereInput] = None,
        cursor: Optional[types.EmbeddingWhereUniqueInput] = None,
        include: Optional[types.EmbeddingInclude] = None,
        order: Optional[Union[types.EmbeddingOrderByInput, List[types.EmbeddingOrderByInput]]] = None,
        distinct: Optional[List[types.EmbeddingScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single Embedding record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Embedding filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Embedding model
        order
            Order the returned Embedding records by any field
        distinct
            Filter Embedding records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Embedding
            The first Embedding record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Embedding record ordered by the dimension field
        embedding = await Embedding.prisma().find_first(
            skip=1,
            order={
                'dimension': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.EmbeddingWhereInput] = None,
        cursor: Optional[types.EmbeddingWhereUniqueInput] = None,
        include: Optional[types.EmbeddingInclude] = None,
        order: Optional[Union[types.EmbeddingOrderByInput, List[types.EmbeddingOrderByInput]]] = None,
        distinct: Optional[List[types.EmbeddingScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single Embedding record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            Embedding filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned Embedding model
        order
            Order the returned Embedding records by any field
        distinct
            Filter Embedding records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.Embedding
            The first Embedding record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second Embedding record ordered by the createdAt field
        embedding = await Embedding.prisma().find_first_or_raise(
            skip=1,
            order={
                'createdAt': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.EmbeddingUpdateInput,
        where: types.EmbeddingWhereUniqueInput,
        include: Optional[types.EmbeddingInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single Embedding record.

        Parameters
        ----------
        data
            Embedding record data specifying what to update
        where
            Embedding filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned Embedding model

        Returns
        -------
        prisma.models.Embedding
            The updated Embedding record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        embedding = await Embedding.prisma().update(
            where={
                'id': 'caaaedabfc',
            },
            data={
                # data to update the Embedding record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.EmbeddingWhereUniqueInput,
        data: types.EmbeddingUpsertInput,
        include: Optional[types.EmbeddingInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            Embedding filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned Embedding model

        Returns
        -------
        prisma.models.Embedding
            The created or updated Embedding record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        embedding = await Embedding.prisma().upsert(
            where={
                'id': 'bigibebcib',
            },
            data={
                'create': {
                    'id': 'bigibebcib',
                    'documentId': 'bbbgbhfjge',
                },
                'update': {
                    'documentId': 'bbbgbhfjge',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.EmbeddingUpdateManyMutationInput,
        where: types.EmbeddingWhereInput,
    ) -> int:
        """Update multiple Embedding records

        Parameters
        ----------
        data
            Embedding data to update the selected Embedding records to
        where
            Filter to select the Embedding records to update

        Returns
        -------
        int
            The total number of Embedding records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all Embedding records
        total = await Embedding.prisma().update_many(
            data={
                'id': 'bigaiehgcc'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmbeddingWhereInput] = None,
        cursor: Optional[types.EmbeddingWhereUniqueInput] = None,
    ) -> int:
        """Count the number of Embedding records present in the database

        Parameters
        ----------
        select
            Select the Embedding fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Embedding filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EmbeddingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Embedding.prisma().count()

        # results: prisma.types.EmbeddingCountAggregateOutput
        results = await Embedding.prisma().count(
            select={
                '_all': True,
                'documentId': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.EmbeddingCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmbeddingWhereInput] = None,
        cursor: Optional[types.EmbeddingWhereUniqueInput] = None,
    ) -> types.EmbeddingCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.EmbeddingCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.EmbeddingWhereInput] = None,
        cursor: Optional[types.EmbeddingWhereUniqueInput] = None,
    ) -> Union[int, types.EmbeddingCountAggregateOutput]:
        """Count the number of Embedding records present in the database

        Parameters
        ----------
        select
            Select the Embedding fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            Embedding filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.EmbeddingCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await Embedding.prisma().count()

        # results: prisma.types.EmbeddingCountAggregateOutput
        results = await Embedding.prisma().count(
            select={
                '_all': True,
                'modelVersionId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.EmbeddingCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.EmbeddingWhereInput] = None
    ) -> int:
        """Delete multiple Embedding records.

        Parameters
        ----------
        where
            Optional Embedding filter to find the records to be deleted

        Returns
        -------
        int
            The total number of Embedding records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all Embedding records
        total = await Embedding.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.EmbeddingScalarFieldKeys'],
        *,
        where: Optional['types.EmbeddingWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.EmbeddingAvgAggregateInput'] = None,
        sum: Optional['types.EmbeddingSumAggregateInput'] = None,
        min: Optional['types.EmbeddingMinAggregateInput'] = None,
        max: Optional['types.EmbeddingMaxAggregateInput'] = None,
        having: Optional['types.EmbeddingScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.EmbeddingCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.EmbeddingScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.EmbeddingScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.EmbeddingGroupByOutput']:
        """Group Embedding records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar Embedding fields to group records by
        where
            Embedding filter to select records
        take
            Limit the maximum number of Embedding records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.EmbeddingGroupByOutput]
            A list of dictionaries representing the Embedding record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group Embedding records by dimension values
        # and count how many records are in each group
        results = await Embedding.prisma().group_by(
            ['dimension'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class DriftWindowActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.DriftWindow]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await DriftWindow.prisma().query_raw(
            'SELECT * FROM DriftWindow WHERE id = $1',
            'beeifcbebf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.DriftWindow
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await DriftWindow.prisma().query_first(
            'SELECT * FROM DriftWindow WHERE windowSize = $1',
            enums.DriftWindowSize.FIVE_MIN,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.DriftWindowCreateInput,
        include: Optional[types.DriftWindowInclude] = None
    ) -> _PrismaModelT:
        """Create a new DriftWindow record.

        Parameters
        ----------
        data
            DriftWindow record data
        include
            Specifies which relations should be loaded on the returned DriftWindow model

        Returns
        -------
        prisma.models.DriftWindow
            The created DriftWindow record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a DriftWindow record from just the required fields
        driftwindow = await DriftWindow.prisma().create(
            data={
                # data to create a DriftWindow record
                'windowSize': enums.DriftWindowSize.FIVE_MIN,
                'windowStart': datetime.datetime.utcnow(),
                'windowEnd': datetime.datetime.utcnow(),
                'documentCount': 1628650740,
                'driftScore': 1249606685.83590,
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.DriftWindowCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple DriftWindow records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of DriftWindow record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await DriftWindow.prisma().create_many(
            data=[
                {
                    # data to create a DriftWindow record
                    'windowSize': enums.DriftWindowSize.FIVE_MIN,
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 763719779,
                    'driftScore': 429995104.177581,
                },
                {
                    # data to create a DriftWindow record
                    'windowSize': enums.DriftWindowSize.FIVE_MIN,
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 893145566,
                    'driftScore': 995405759.210273,
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.DriftWindowWhereUniqueInput,
        include: Optional[types.DriftWindowInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single DriftWindow record.

        Parameters
        ----------
        where
            DriftWindow filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned DriftWindow model

        Returns
        -------
        prisma.models.DriftWindow
            The deleted DriftWindow record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        driftwindow = await DriftWindow.prisma().delete(
            where={
                'id': 'chbfcacbd',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.DriftWindowWhereUniqueInput,
        include: Optional[types.DriftWindowInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique DriftWindow record.

        Parameters
        ----------
        where
            DriftWindow filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DriftWindow model

        Returns
        -------
        prisma.models.DriftWindow
            The found DriftWindow record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        driftwindow = await DriftWindow.prisma().find_unique(
            where={
                'id': 'efggddide',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.DriftWindowWhereUniqueInput,
        include: Optional[types.DriftWindowInclude] = None
    ) -> _PrismaModelT:
        """Find a unique DriftWindow record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            DriftWindow filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned DriftWindow model

        Returns
        -------
        prisma.models.DriftWindow
            The found DriftWindow record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        driftwindow = await DriftWindow.prisma().find_unique_or_raise(
            where={
                'id': 'caficfigfb',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DriftWindowWhereInput] = None,
        cursor: Optional[types.DriftWindowWhereUniqueInput] = None,
        include: Optional[types.DriftWindowInclude] = None,
        order: Optional[Union[types.DriftWindowOrderByInput, List[types.DriftWindowOrderByInput]]] = None,
        distinct: Optional[List[types.DriftWindowScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple DriftWindow records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of DriftWindow records returned
        skip
            Ignore the first N results
        where
            DriftWindow filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DriftWindow model
        order
            Order the returned DriftWindow records by any field
        distinct
            Filter DriftWindow records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.DriftWindow]
            The list of all DriftWindow records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 DriftWindow records
        driftwindows = await DriftWindow.prisma().find_many(take=10)

        # find the first 5 DriftWindow records ordered by the windowStart field
        driftwindows = await DriftWindow.prisma().find_many(
            take=5,
            order={
                'windowStart': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DriftWindowWhereInput] = None,
        cursor: Optional[types.DriftWindowWhereUniqueInput] = None,
        include: Optional[types.DriftWindowInclude] = None,
        order: Optional[Union[types.DriftWindowOrderByInput, List[types.DriftWindowOrderByInput]]] = None,
        distinct: Optional[List[types.DriftWindowScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single DriftWindow record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DriftWindow filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DriftWindow model
        order
            Order the returned DriftWindow records by any field
        distinct
            Filter DriftWindow records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DriftWindow
            The first DriftWindow record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DriftWindow record ordered by the windowEnd field
        driftwindow = await DriftWindow.prisma().find_first(
            skip=1,
            order={
                'windowEnd': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.DriftWindowWhereInput] = None,
        cursor: Optional[types.DriftWindowWhereUniqueInput] = None,
        include: Optional[types.DriftWindowInclude] = None,
        order: Optional[Union[types.DriftWindowOrderByInput, List[types.DriftWindowOrderByInput]]] = None,
        distinct: Optional[List[types.DriftWindowScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single DriftWindow record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            DriftWindow filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned DriftWindow model
        order
            Order the returned DriftWindow records by any field
        distinct
            Filter DriftWindow records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.DriftWindow
            The first DriftWindow record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second DriftWindow record ordered by the documentCount field
        driftwindow = await DriftWindow.prisma().find_first_or_raise(
            skip=1,
            order={
                'documentCount': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.DriftWindowUpdateInput,
        where: types.DriftWindowWhereUniqueInput,
        include: Optional[types.DriftWindowInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single DriftWindow record.

        Parameters
        ----------
        data
            DriftWindow record data specifying what to update
        where
            DriftWindow filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned DriftWindow model

        Returns
        -------
        prisma.models.DriftWindow
            The updated DriftWindow record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        driftwindow = await DriftWindow.prisma().update(
            where={
                'id': 'bfidgijfjc',
            },
            data={
                # data to update the DriftWindow record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.DriftWindowWhereUniqueInput,
        data: types.DriftWindowUpsertInput,
        include: Optional[types.DriftWindowInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            DriftWindow filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned DriftWindow model

        Returns
        -------
        prisma.models.DriftWindow
            The created or updated DriftWindow record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        driftwindow = await DriftWindow.prisma().upsert(
            where={
                'id': 'ihieecagf',
            },
            data={
                'create': {
                    'id': 'ihieecagf',
                    'windowSize': enums.DriftWindowSize.FIVE_MIN,
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 893145566,
                    'driftScore': 995405759.210273,
                },
                'update': {
                    'windowSize': enums.DriftWindowSize.FIVE_MIN,
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 893145566,
                    'driftScore': 995405759.210273,
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.DriftWindowUpdateManyMutationInput,
        where: types.DriftWindowWhereInput,
    ) -> int:
        """Update multiple DriftWindow records

        Parameters
        ----------
        data
            DriftWindow data to update the selected DriftWindow records to
        where
            Filter to select the DriftWindow records to update

        Returns
        -------
        int
            The total number of DriftWindow records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all DriftWindow records
        total = await DriftWindow.prisma().update_many(
            data={
                'klDivergence': 1675280054.162757
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DriftWindowWhereInput] = None,
        cursor: Optional[types.DriftWindowWhereUniqueInput] = None,
    ) -> int:
        """Count the number of DriftWindow records present in the database

        Parameters
        ----------
        select
            Select the DriftWindow fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DriftWindow filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DriftWindowCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DriftWindow.prisma().count()

        # results: prisma.types.DriftWindowCountAggregateOutput
        results = await DriftWindow.prisma().count(
            select={
                '_all': True,
                'wassersteinDistance': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.DriftWindowCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DriftWindowWhereInput] = None,
        cursor: Optional[types.DriftWindowWhereUniqueInput] = None,
    ) -> types.DriftWindowCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.DriftWindowCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.DriftWindowWhereInput] = None,
        cursor: Optional[types.DriftWindowWhereUniqueInput] = None,
    ) -> Union[int, types.DriftWindowCountAggregateOutput]:
        """Count the number of DriftWindow records present in the database

        Parameters
        ----------
        select
            Select the DriftWindow fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            DriftWindow filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.DriftWindowCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await DriftWindow.prisma().count()

        # results: prisma.types.DriftWindowCountAggregateOutput
        results = await DriftWindow.prisma().count(
            select={
                '_all': True,
                'driftScore': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.DriftWindowCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.DriftWindowWhereInput] = None
    ) -> int:
        """Delete multiple DriftWindow records.

        Parameters
        ----------
        where
            Optional DriftWindow filter to find the records to be deleted

        Returns
        -------
        int
            The total number of DriftWindow records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all DriftWindow records
        total = await DriftWindow.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.DriftWindowScalarFieldKeys'],
        *,
        where: Optional['types.DriftWindowWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.DriftWindowAvgAggregateInput'] = None,
        sum: Optional['types.DriftWindowSumAggregateInput'] = None,
        min: Optional['types.DriftWindowMinAggregateInput'] = None,
        max: Optional['types.DriftWindowMaxAggregateInput'] = None,
        having: Optional['types.DriftWindowScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.DriftWindowCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.DriftWindowScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.DriftWindowScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.DriftWindowGroupByOutput']:
        """Group DriftWindow records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar DriftWindow fields to group records by
        where
            DriftWindow filter to select records
        take
            Limit the maximum number of DriftWindow records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.DriftWindowGroupByOutput]
            A list of dictionaries representing the DriftWindow record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group DriftWindow records by threshold values
        # and count how many records are in each group
        results = await DriftWindow.prisma().group_by(
            ['threshold'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class LinguisticWindowActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.LinguisticWindow]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await LinguisticWindow.prisma().query_raw(
            'SELECT * FROM LinguisticWindow WHERE id = $1',
            'cafeiaccbc',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.LinguisticWindow
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await LinguisticWindow.prisma().query_first(
            'SELECT * FROM LinguisticWindow WHERE windowStart = $1',
            datetime.datetime.utcnow(),
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.LinguisticWindowCreateInput,
        include: Optional[types.LinguisticWindowInclude] = None
    ) -> _PrismaModelT:
        """Create a new LinguisticWindow record.

        Parameters
        ----------
        data
            LinguisticWindow record data
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model

        Returns
        -------
        prisma.models.LinguisticWindow
            The created LinguisticWindow record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a LinguisticWindow record from just the required fields
        linguisticwindow = await LinguisticWindow.prisma().create(
            data={
                # data to create a LinguisticWindow record
                'windowStart': datetime.datetime.utcnow(),
                'windowEnd': datetime.datetime.utcnow(),
                'documentCount': 60335757,
                'entityKlDivergence': 684462146.162550,
                'topicWasserstein': 521827728.126603,
                'vocabChi2Pvalue': 93253262.205304,
                'compositeScore': 685333180.12747,
                'newEntities': Json({'jeijcbhfe': True}),
                'emergingTopics': Json({'bjgejjabff': True}),
                'emergingTerms': Json({'bcciijbibg': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.LinguisticWindowCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple LinguisticWindow records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of LinguisticWindow record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await LinguisticWindow.prisma().create_many(
            data=[
                {
                    # data to create a LinguisticWindow record
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 255202753,
                    'entityKlDivergence': 1223573862.54126,
                    'topicWasserstein': 1064846676.50838,
                    'vocabChi2Pvalue': 1024265714.87207,
                    'compositeScore': 1874748096.91689,
                    'newEntities': Json({'hgjcghfbi': True}),
                    'emergingTopics': Json({'icadbcehj': True}),
                    'emergingTerms': Json({'jchciaee': True}),
                },
                {
                    # data to create a LinguisticWindow record
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 344858293,
                    'entityKlDivergence': 1121741130.149589,
                    'topicWasserstein': 208521688.86081,
                    'vocabChi2Pvalue': 1660932118.52576,
                    'compositeScore': 736209796.49390,
                    'newEntities': Json({'gdjgigfgc': True}),
                    'emergingTopics': Json({'gfeaahdeh': True}),
                    'emergingTerms': Json({'bjafcgbffc': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.LinguisticWindowWhereUniqueInput,
        include: Optional[types.LinguisticWindowInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single LinguisticWindow record.

        Parameters
        ----------
        where
            LinguisticWindow filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model

        Returns
        -------
        prisma.models.LinguisticWindow
            The deleted LinguisticWindow record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        linguisticwindow = await LinguisticWindow.prisma().delete(
            where={
                'id': 'hihegjif',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.LinguisticWindowWhereUniqueInput,
        include: Optional[types.LinguisticWindowInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique LinguisticWindow record.

        Parameters
        ----------
        where
            LinguisticWindow filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model

        Returns
        -------
        prisma.models.LinguisticWindow
            The found LinguisticWindow record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        linguisticwindow = await LinguisticWindow.prisma().find_unique(
            where={
                'id': 'bdjidcidac',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.LinguisticWindowWhereUniqueInput,
        include: Optional[types.LinguisticWindowInclude] = None
    ) -> _PrismaModelT:
        """Find a unique LinguisticWindow record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            LinguisticWindow filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model

        Returns
        -------
        prisma.models.LinguisticWindow
            The found LinguisticWindow record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        linguisticwindow = await LinguisticWindow.prisma().find_unique_or_raise(
            where={
                'id': 'ifgaaagff',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LinguisticWindowWhereInput] = None,
        cursor: Optional[types.LinguisticWindowWhereUniqueInput] = None,
        include: Optional[types.LinguisticWindowInclude] = None,
        order: Optional[Union[types.LinguisticWindowOrderByInput, List[types.LinguisticWindowOrderByInput]]] = None,
        distinct: Optional[List[types.LinguisticWindowScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple LinguisticWindow records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of LinguisticWindow records returned
        skip
            Ignore the first N results
        where
            LinguisticWindow filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model
        order
            Order the returned LinguisticWindow records by any field
        distinct
            Filter LinguisticWindow records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.LinguisticWindow]
            The list of all LinguisticWindow records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 LinguisticWindow records
        linguisticwindows = await LinguisticWindow.prisma().find_many(take=10)

        # find the first 5 LinguisticWindow records ordered by the windowEnd field
        linguisticwindows = await LinguisticWindow.prisma().find_many(
            take=5,
            order={
                'windowEnd': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LinguisticWindowWhereInput] = None,
        cursor: Optional[types.LinguisticWindowWhereUniqueInput] = None,
        include: Optional[types.LinguisticWindowInclude] = None,
        order: Optional[Union[types.LinguisticWindowOrderByInput, List[types.LinguisticWindowOrderByInput]]] = None,
        distinct: Optional[List[types.LinguisticWindowScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single LinguisticWindow record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LinguisticWindow filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model
        order
            Order the returned LinguisticWindow records by any field
        distinct
            Filter LinguisticWindow records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LinguisticWindow
            The first LinguisticWindow record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LinguisticWindow record ordered by the documentCount field
        linguisticwindow = await LinguisticWindow.prisma().find_first(
            skip=1,
            order={
                'documentCount': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.LinguisticWindowWhereInput] = None,
        cursor: Optional[types.LinguisticWindowWhereUniqueInput] = None,
        include: Optional[types.LinguisticWindowInclude] = None,
        order: Optional[Union[types.LinguisticWindowOrderByInput, List[types.LinguisticWindowOrderByInput]]] = None,
        distinct: Optional[List[types.LinguisticWindowScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single LinguisticWindow record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            LinguisticWindow filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model
        order
            Order the returned LinguisticWindow records by any field
        distinct
            Filter LinguisticWindow records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.LinguisticWindow
            The first LinguisticWindow record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second LinguisticWindow record ordered by the entityKlDivergence field
        linguisticwindow = await LinguisticWindow.prisma().find_first_or_raise(
            skip=1,
            order={
                'entityKlDivergence': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.LinguisticWindowUpdateInput,
        where: types.LinguisticWindowWhereUniqueInput,
        include: Optional[types.LinguisticWindowInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single LinguisticWindow record.

        Parameters
        ----------
        data
            LinguisticWindow record data specifying what to update
        where
            LinguisticWindow filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model

        Returns
        -------
        prisma.models.LinguisticWindow
            The updated LinguisticWindow record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        linguisticwindow = await LinguisticWindow.prisma().update(
            where={
                'id': 'befcddgjce',
            },
            data={
                # data to update the LinguisticWindow record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.LinguisticWindowWhereUniqueInput,
        data: types.LinguisticWindowUpsertInput,
        include: Optional[types.LinguisticWindowInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            LinguisticWindow filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned LinguisticWindow model

        Returns
        -------
        prisma.models.LinguisticWindow
            The created or updated LinguisticWindow record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        linguisticwindow = await LinguisticWindow.prisma().upsert(
            where={
                'id': 'bfhdbjjgfd',
            },
            data={
                'create': {
                    'id': 'bfhdbjjgfd',
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 344858293,
                    'entityKlDivergence': 1121741130.149589,
                    'topicWasserstein': 208521688.86081,
                    'vocabChi2Pvalue': 1660932118.52576,
                    'compositeScore': 736209796.49390,
                    'newEntities': Json({'gdjgigfgc': True}),
                    'emergingTopics': Json({'gfeaahdeh': True}),
                    'emergingTerms': Json({'bjafcgbffc': True}),
                },
                'update': {
                    'windowStart': datetime.datetime.utcnow(),
                    'windowEnd': datetime.datetime.utcnow(),
                    'documentCount': 344858293,
                    'entityKlDivergence': 1121741130.149589,
                    'topicWasserstein': 208521688.86081,
                    'vocabChi2Pvalue': 1660932118.52576,
                    'compositeScore': 736209796.49390,
                    'newEntities': Json({'gdjgigfgc': True}),
                    'emergingTopics': Json({'gfeaahdeh': True}),
                    'emergingTerms': Json({'bjafcgbffc': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.LinguisticWindowUpdateManyMutationInput,
        where: types.LinguisticWindowWhereInput,
    ) -> int:
        """Update multiple LinguisticWindow records

        Parameters
        ----------
        data
            LinguisticWindow data to update the selected LinguisticWindow records to
        where
            Filter to select the LinguisticWindow records to update

        Returns
        -------
        int
            The total number of LinguisticWindow records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all LinguisticWindow records
        total = await LinguisticWindow.prisma().update_many(
            data={
                'topicWasserstein': 2013903098.50096
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LinguisticWindowWhereInput] = None,
        cursor: Optional[types.LinguisticWindowWhereUniqueInput] = None,
    ) -> int:
        """Count the number of LinguisticWindow records present in the database

        Parameters
        ----------
        select
            Select the LinguisticWindow fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LinguisticWindow filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LinguisticWindowCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LinguisticWindow.prisma().count()

        # results: prisma.types.LinguisticWindowCountAggregateOutput
        results = await LinguisticWindow.prisma().count(
            select={
                '_all': True,
                'vocabChi2Pvalue': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.LinguisticWindowCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LinguisticWindowWhereInput] = None,
        cursor: Optional[types.LinguisticWindowWhereUniqueInput] = None,
    ) -> types.LinguisticWindowCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.LinguisticWindowCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.LinguisticWindowWhereInput] = None,
        cursor: Optional[types.LinguisticWindowWhereUniqueInput] = None,
    ) -> Union[int, types.LinguisticWindowCountAggregateOutput]:
        """Count the number of LinguisticWindow records present in the database

        Parameters
        ----------
        select
            Select the LinguisticWindow fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            LinguisticWindow filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.LinguisticWindowCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await LinguisticWindow.prisma().count()

        # results: prisma.types.LinguisticWindowCountAggregateOutput
        results = await LinguisticWindow.prisma().count(
            select={
                '_all': True,
                'compositeScore': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.LinguisticWindowCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.LinguisticWindowWhereInput] = None
    ) -> int:
        """Delete multiple LinguisticWindow records.

        Parameters
        ----------
        where
            Optional LinguisticWindow filter to find the records to be deleted

        Returns
        -------
        int
            The total number of LinguisticWindow records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all LinguisticWindow records
        total = await LinguisticWindow.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.LinguisticWindowScalarFieldKeys'],
        *,
        where: Optional['types.LinguisticWindowWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.LinguisticWindowAvgAggregateInput'] = None,
        sum: Optional['types.LinguisticWindowSumAggregateInput'] = None,
        min: Optional['types.LinguisticWindowMinAggregateInput'] = None,
        max: Optional['types.LinguisticWindowMaxAggregateInput'] = None,
        having: Optional['types.LinguisticWindowScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.LinguisticWindowCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.LinguisticWindowScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.LinguisticWindowScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.LinguisticWindowGroupByOutput']:
        """Group LinguisticWindow records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar LinguisticWindow fields to group records by
        where
            LinguisticWindow filter to select records
        take
            Limit the maximum number of LinguisticWindow records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.LinguisticWindowGroupByOutput]
            A list of dictionaries representing the LinguisticWindow record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group LinguisticWindow records by threshold values
        # and count how many records are in each group
        results = await LinguisticWindow.prisma().group_by(
            ['threshold'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class ModelVersionActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.ModelVersion]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await ModelVersion.prisma().query_raw(
            'SELECT * FROM ModelVersion WHERE id = $1',
            'biaagcedjc',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.ModelVersion
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await ModelVersion.prisma().query_first(
            'SELECT * FROM ModelVersion WHERE version = $1',
            'cahhaghecf',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.ModelVersionCreateInput,
        include: Optional[types.ModelVersionInclude] = None
    ) -> _PrismaModelT:
        """Create a new ModelVersion record.

        Parameters
        ----------
        data
            ModelVersion record data
        include
            Specifies which relations should be loaded on the returned ModelVersion model

        Returns
        -------
        prisma.models.ModelVersion
            The created ModelVersion record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a ModelVersion record from just the required fields
        modelversion = await ModelVersion.prisma().create(
            data={
                # data to create a ModelVersion record
                'version': 'bghcbbcidi',
                'baseModel': 'jcgghhgdj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.ModelVersionCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple ModelVersion records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of ModelVersion record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await ModelVersion.prisma().create_many(
            data=[
                {
                    # data to create a ModelVersion record
                    'version': 'beehgcebbg',
                    'baseModel': 'bhdiaidiaf',
                },
                {
                    # data to create a ModelVersion record
                    'version': 'deajegcfi',
                    'baseModel': 'gabahhhjf',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.ModelVersionWhereUniqueInput,
        include: Optional[types.ModelVersionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single ModelVersion record.

        Parameters
        ----------
        where
            ModelVersion filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned ModelVersion model

        Returns
        -------
        prisma.models.ModelVersion
            The deleted ModelVersion record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        modelversion = await ModelVersion.prisma().delete(
            where={
                'id': 'cjagadcjg',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.ModelVersionWhereUniqueInput,
        include: Optional[types.ModelVersionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique ModelVersion record.

        Parameters
        ----------
        where
            ModelVersion filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ModelVersion model

        Returns
        -------
        prisma.models.ModelVersion
            The found ModelVersion record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        modelversion = await ModelVersion.prisma().find_unique(
            where={
                'id': 'bifficggej',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.ModelVersionWhereUniqueInput,
        include: Optional[types.ModelVersionInclude] = None
    ) -> _PrismaModelT:
        """Find a unique ModelVersion record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            ModelVersion filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned ModelVersion model

        Returns
        -------
        prisma.models.ModelVersion
            The found ModelVersion record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        modelversion = await ModelVersion.prisma().find_unique_or_raise(
            where={
                'id': 'bgbbaajbic',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ModelVersionWhereInput] = None,
        cursor: Optional[types.ModelVersionWhereUniqueInput] = None,
        include: Optional[types.ModelVersionInclude] = None,
        order: Optional[Union[types.ModelVersionOrderByInput, List[types.ModelVersionOrderByInput]]] = None,
        distinct: Optional[List[types.ModelVersionScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple ModelVersion records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of ModelVersion records returned
        skip
            Ignore the first N results
        where
            ModelVersion filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ModelVersion model
        order
            Order the returned ModelVersion records by any field
        distinct
            Filter ModelVersion records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.ModelVersion]
            The list of all ModelVersion records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 ModelVersion records
        modelversions = await ModelVersion.prisma().find_many(take=10)

        # find the first 5 ModelVersion records ordered by the baseModel field
        modelversions = await ModelVersion.prisma().find_many(
            take=5,
            order={
                'baseModel': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ModelVersionWhereInput] = None,
        cursor: Optional[types.ModelVersionWhereUniqueInput] = None,
        include: Optional[types.ModelVersionInclude] = None,
        order: Optional[Union[types.ModelVersionOrderByInput, List[types.ModelVersionOrderByInput]]] = None,
        distinct: Optional[List[types.ModelVersionScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single ModelVersion record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ModelVersion filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ModelVersion model
        order
            Order the returned ModelVersion records by any field
        distinct
            Filter ModelVersion records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ModelVersion
            The first ModelVersion record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ModelVersion record ordered by the status field
        modelversion = await ModelVersion.prisma().find_first(
            skip=1,
            order={
                'status': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.ModelVersionWhereInput] = None,
        cursor: Optional[types.ModelVersionWhereUniqueInput] = None,
        include: Optional[types.ModelVersionInclude] = None,
        order: Optional[Union[types.ModelVersionOrderByInput, List[types.ModelVersionOrderByInput]]] = None,
        distinct: Optional[List[types.ModelVersionScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single ModelVersion record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            ModelVersion filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned ModelVersion model
        order
            Order the returned ModelVersion records by any field
        distinct
            Filter ModelVersion records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.ModelVersion
            The first ModelVersion record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second ModelVersion record ordered by the artifactUri field
        modelversion = await ModelVersion.prisma().find_first_or_raise(
            skip=1,
            order={
                'artifactUri': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.ModelVersionUpdateInput,
        where: types.ModelVersionWhereUniqueInput,
        include: Optional[types.ModelVersionInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single ModelVersion record.

        Parameters
        ----------
        data
            ModelVersion record data specifying what to update
        where
            ModelVersion filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned ModelVersion model

        Returns
        -------
        prisma.models.ModelVersion
            The updated ModelVersion record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        modelversion = await ModelVersion.prisma().update(
            where={
                'id': 'eegghdhjb',
            },
            data={
                # data to update the ModelVersion record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.ModelVersionWhereUniqueInput,
        data: types.ModelVersionUpsertInput,
        include: Optional[types.ModelVersionInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            ModelVersion filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned ModelVersion model

        Returns
        -------
        prisma.models.ModelVersion
            The created or updated ModelVersion record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        modelversion = await ModelVersion.prisma().upsert(
            where={
                'id': 'daafgidjg',
            },
            data={
                'create': {
                    'id': 'daafgidjg',
                    'version': 'deajegcfi',
                    'baseModel': 'gabahhhjf',
                },
                'update': {
                    'version': 'deajegcfi',
                    'baseModel': 'gabahhhjf',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.ModelVersionUpdateManyMutationInput,
        where: types.ModelVersionWhereInput,
    ) -> int:
        """Update multiple ModelVersion records

        Parameters
        ----------
        data
            ModelVersion data to update the selected ModelVersion records to
        where
            Filter to select the ModelVersion records to update

        Returns
        -------
        int
            The total number of ModelVersion records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all ModelVersion records
        total = await ModelVersion.prisma().update_many(
            data={
                'artifactSha256': 'gdcgcgagj'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ModelVersionWhereInput] = None,
        cursor: Optional[types.ModelVersionWhereUniqueInput] = None,
    ) -> int:
        """Count the number of ModelVersion records present in the database

        Parameters
        ----------
        select
            Select the ModelVersion fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ModelVersion filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ModelVersionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ModelVersion.prisma().count()

        # results: prisma.types.ModelVersionCountAggregateOutput
        results = await ModelVersion.prisma().count(
            select={
                '_all': True,
                'artifactBytes': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.ModelVersionCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ModelVersionWhereInput] = None,
        cursor: Optional[types.ModelVersionWhereUniqueInput] = None,
    ) -> types.ModelVersionCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.ModelVersionCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.ModelVersionWhereInput] = None,
        cursor: Optional[types.ModelVersionWhereUniqueInput] = None,
    ) -> Union[int, types.ModelVersionCountAggregateOutput]:
        """Count the number of ModelVersion records present in the database

        Parameters
        ----------
        select
            Select the ModelVersion fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            ModelVersion filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.ModelVersionCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await ModelVersion.prisma().count()

        # results: prisma.types.ModelVersionCountAggregateOutput
        results = await ModelVersion.prisma().count(
            select={
                '_all': True,
                'domainTag': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.ModelVersionCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.ModelVersionWhereInput] = None
    ) -> int:
        """Delete multiple ModelVersion records.

        Parameters
        ----------
        where
            Optional ModelVersion filter to find the records to be deleted

        Returns
        -------
        int
            The total number of ModelVersion records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all ModelVersion records
        total = await ModelVersion.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.ModelVersionScalarFieldKeys'],
        *,
        where: Optional['types.ModelVersionWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.ModelVersionAvgAggregateInput'] = None,
        sum: Optional['types.ModelVersionSumAggregateInput'] = None,
        min: Optional['types.ModelVersionMinAggregateInput'] = None,
        max: Optional['types.ModelVersionMaxAggregateInput'] = None,
        having: Optional['types.ModelVersionScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.ModelVersionCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.ModelVersionScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.ModelVersionScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.ModelVersionGroupByOutput']:
        """Group ModelVersion records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar ModelVersion fields to group records by
        where
            ModelVersion filter to select records
        take
            Limit the maximum number of ModelVersion records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.ModelVersionGroupByOutput]
            A list of dictionaries representing the ModelVersion record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group ModelVersion records by onnxPath values
        # and count how many records are in each group
        results = await ModelVersion.prisma().group_by(
            ['onnxPath'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TrainingJobActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TrainingJob]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await TrainingJob.prisma().query_raw(
            'SELECT * FROM TrainingJob WHERE id = $1',
            'bhceabbgja',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TrainingJob
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await TrainingJob.prisma().query_first(
            'SELECT * FROM TrainingJob WHERE status = $1',
            enums.TrainingJobStatus.QUEUED,
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.TrainingJobCreateInput,
        include: Optional[types.TrainingJobInclude] = None
    ) -> _PrismaModelT:
        """Create a new TrainingJob record.

        Parameters
        ----------
        data
            TrainingJob record data
        include
            Specifies which relations should be loaded on the returned TrainingJob model

        Returns
        -------
        prisma.models.TrainingJob
            The created TrainingJob record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TrainingJob record from just the required fields
        trainingjob = await TrainingJob.prisma().create(
            data={
                # data to create a TrainingJob record
                'trigger': enums.TrainingTrigger.DRIFT_ALERT,
                'baseModel': 'ehabfhegh',
                'hyperparameters': Json({'bcajcajjbc': True}),
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.TrainingJobCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TrainingJob records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TrainingJob record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await TrainingJob.prisma().create_many(
            data=[
                {
                    # data to create a TrainingJob record
                    'trigger': enums.TrainingTrigger.DRIFT_ALERT,
                    'baseModel': 'bfdgheeegf',
                    'hyperparameters': Json({'ececbijji': True}),
                },
                {
                    # data to create a TrainingJob record
                    'trigger': enums.TrainingTrigger.DRIFT_ALERT,
                    'baseModel': 'cbcfgdcdhf',
                    'hyperparameters': Json({'fdgjfbhia': True}),
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.TrainingJobWhereUniqueInput,
        include: Optional[types.TrainingJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TrainingJob record.

        Parameters
        ----------
        where
            TrainingJob filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TrainingJob model

        Returns
        -------
        prisma.models.TrainingJob
            The deleted TrainingJob record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        trainingjob = await TrainingJob.prisma().delete(
            where={
                'id': 'jcehcdchh',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.TrainingJobWhereUniqueInput,
        include: Optional[types.TrainingJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TrainingJob record.

        Parameters
        ----------
        where
            TrainingJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TrainingJob model

        Returns
        -------
        prisma.models.TrainingJob
            The found TrainingJob record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        trainingjob = await TrainingJob.prisma().find_unique(
            where={
                'id': 'bgcbjdhjcc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.TrainingJobWhereUniqueInput,
        include: Optional[types.TrainingJobInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TrainingJob record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TrainingJob filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TrainingJob model

        Returns
        -------
        prisma.models.TrainingJob
            The found TrainingJob record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        trainingjob = await TrainingJob.prisma().find_unique_or_raise(
            where={
                'id': 'bieiidcabj',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingJobWhereInput] = None,
        cursor: Optional[types.TrainingJobWhereUniqueInput] = None,
        include: Optional[types.TrainingJobInclude] = None,
        order: Optional[Union[types.TrainingJobOrderByInput, List[types.TrainingJobOrderByInput]]] = None,
        distinct: Optional[List[types.TrainingJobScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TrainingJob records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TrainingJob records returned
        skip
            Ignore the first N results
        where
            TrainingJob filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TrainingJob model
        order
            Order the returned TrainingJob records by any field
        distinct
            Filter TrainingJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TrainingJob]
            The list of all TrainingJob records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TrainingJob records
        trainingjobs = await TrainingJob.prisma().find_many(take=10)

        # find the first 5 TrainingJob records ordered by the trigger field
        trainingjobs = await TrainingJob.prisma().find_many(
            take=5,
            order={
                'trigger': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TrainingJobWhereInput] = None,
        cursor: Optional[types.TrainingJobWhereUniqueInput] = None,
        include: Optional[types.TrainingJobInclude] = None,
        order: Optional[Union[types.TrainingJobOrderByInput, List[types.TrainingJobOrderByInput]]] = None,
        distinct: Optional[List[types.TrainingJobScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TrainingJob record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TrainingJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TrainingJob model
        order
            Order the returned TrainingJob records by any field
        distinct
            Filter TrainingJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TrainingJob
            The first TrainingJob record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TrainingJob record ordered by the driftWindowId field
        trainingjob = await TrainingJob.prisma().find_first(
            skip=1,
            order={
                'driftWindowId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TrainingJobWhereInput] = None,
        cursor: Optional[types.TrainingJobWhereUniqueInput] = None,
        include: Optional[types.TrainingJobInclude] = None,
        order: Optional[Union[types.TrainingJobOrderByInput, List[types.TrainingJobOrderByInput]]] = None,
        distinct: Optional[List[types.TrainingJobScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TrainingJob record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TrainingJob filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TrainingJob model
        order
            Order the returned TrainingJob records by any field
        distinct
            Filter TrainingJob records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TrainingJob
            The first TrainingJob record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TrainingJob record ordered by the modelVersionId field
        trainingjob = await TrainingJob.prisma().find_first_or_raise(
            skip=1,
            order={
                'modelVersionId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.TrainingJobUpdateInput,
        where: types.TrainingJobWhereUniqueInput,
        include: Optional[types.TrainingJobInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TrainingJob record.

        Parameters
        ----------
        data
            TrainingJob record data specifying what to update
        where
            TrainingJob filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TrainingJob model

        Returns
        -------
        prisma.models.TrainingJob
            The updated TrainingJob record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        trainingjob = await TrainingJob.prisma().update(
            where={
                'id': 'bjcbfcieaa',
            },
            data={
                # data to update the TrainingJob record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.TrainingJobWhereUniqueInput,
        data: types.TrainingJobUpsertInput,
        include: Optional[types.TrainingJobInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TrainingJob filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TrainingJob model

        Returns
        -------
        prisma.models.TrainingJob
            The created or updated TrainingJob record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        trainingjob = await TrainingJob.prisma().upsert(
            where={
                'id': 'cbaaechiej',
            },
            data={
                'create': {
                    'id': 'cbaaechiej',
                    'trigger': enums.TrainingTrigger.DRIFT_ALERT,
                    'baseModel': 'cbcfgdcdhf',
                    'hyperparameters': Json({'fdgjfbhia': True}),
                },
                'update': {
                    'trigger': enums.TrainingTrigger.DRIFT_ALERT,
                    'baseModel': 'cbcfgdcdhf',
                    'hyperparameters': Json({'fdgjfbhia': True}),
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.TrainingJobUpdateManyMutationInput,
        where: types.TrainingJobWhereInput,
    ) -> int:
        """Update multiple TrainingJob records

        Parameters
        ----------
        data
            TrainingJob data to update the selected TrainingJob records to
        where
            Filter to select the TrainingJob records to update

        Returns
        -------
        int
            The total number of TrainingJob records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TrainingJob records
        total = await TrainingJob.prisma().update_many(
            data={
                'baseModel': 'iejbeaaeg'
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingJobWhereInput] = None,
        cursor: Optional[types.TrainingJobWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TrainingJob records present in the database

        Parameters
        ----------
        select
            Select the TrainingJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TrainingJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TrainingJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await TrainingJob.prisma().count()

        # results: prisma.types.TrainingJobCountAggregateOutput
        results = await TrainingJob.prisma().count(
            select={
                '_all': True,
                'hyperparameters': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.TrainingJobCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingJobWhereInput] = None,
        cursor: Optional[types.TrainingJobWhereUniqueInput] = None,
    ) -> types.TrainingJobCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.TrainingJobCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingJobWhereInput] = None,
        cursor: Optional[types.TrainingJobWhereUniqueInput] = None,
    ) -> Union[int, types.TrainingJobCountAggregateOutput]:
        """Count the number of TrainingJob records present in the database

        Parameters
        ----------
        select
            Select the TrainingJob fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TrainingJob filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TrainingJobCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await TrainingJob.prisma().count()

        # results: prisma.types.TrainingJobCountAggregateOutput
        results = await TrainingJob.prisma().count(
            select={
                '_all': True,
                'sampleCount': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TrainingJobCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.TrainingJobWhereInput] = None
    ) -> int:
        """Delete multiple TrainingJob records.

        Parameters
        ----------
        where
            Optional TrainingJob filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TrainingJob records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TrainingJob records
        total = await TrainingJob.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.TrainingJobScalarFieldKeys'],
        *,
        where: Optional['types.TrainingJobWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TrainingJobAvgAggregateInput'] = None,
        sum: Optional['types.TrainingJobSumAggregateInput'] = None,
        min: Optional['types.TrainingJobMinAggregateInput'] = None,
        max: Optional['types.TrainingJobMaxAggregateInput'] = None,
        having: Optional['types.TrainingJobScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TrainingJobCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TrainingJobScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TrainingJobScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TrainingJobGroupByOutput']:
        """Group TrainingJob records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TrainingJob fields to group records by
        where
            TrainingJob filter to select records
        take
            Limit the maximum number of TrainingJob records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TrainingJobGroupByOutput]
            A list of dictionaries representing the TrainingJob record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TrainingJob records by attempts values
        # and count how many records are in each group
        results = await TrainingJob.prisma().group_by(
            ['attempts'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]


class TrainingLinguisticSignalActions(Generic[_PrismaModelT]):
    __slots__ = (
        '_client',
        '_model',
    )

    def __init__(self, client: Prisma, model: Type[_PrismaModelT]) -> None:
        self._client = client
        self._model = model

    async def query_raw(
        self,
        query: LiteralString,
        *args: Any,
    ) -> List[_PrismaModelT]:
        """Execute a raw SQL query

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        List[prisma.models.TrainingLinguisticSignal]
            The records returned by the SQL query

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        users = await TrainingLinguisticSignal.prisma().query_raw(
            'SELECT * FROM TrainingLinguisticSignal WHERE id = $1',
            'jcibfcbhf',
        )
        ```
        """
        return await self._client.query_raw(query, *args, model=self._model)

    async def query_first(
        self,
        query: LiteralString,
        *args: Any,
    ) -> Optional[_PrismaModelT]:
        """Execute a raw SQL query, returning the first result

        Parameters
        ----------
        query
            The raw SQL query string to be executed
        *args
            Parameters to be passed to the SQL query, these MUST be used over
            string formatting to avoid an SQL injection vulnerability

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The first record returned by the SQL query
        None
            The raw SQL query did not return any records

        Raises
        ------
        prisma_errors.RawQueryError
            This could be due to invalid syntax, mismatched number of parameters or any other error
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        user = await TrainingLinguisticSignal.prisma().query_first(
            'SELECT * FROM TrainingLinguisticSignal WHERE trainingJobId = $1',
            'chdadcaga',
        )
        ```
        """
        return await self._client.query_first(query, *args, model=self._model)

    async def create(
        self,
        data: types.TrainingLinguisticSignalCreateInput,
        include: Optional[types.TrainingLinguisticSignalInclude] = None
    ) -> _PrismaModelT:
        """Create a new TrainingLinguisticSignal record.

        Parameters
        ----------
        data
            TrainingLinguisticSignal record data
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The created TrainingLinguisticSignal record

        Raises
        ------
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # create a TrainingLinguisticSignal record from just the required fields
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().create(
            data={
                # data to create a TrainingLinguisticSignal record
                'trainingJobId': 'jicieifbh',
                'linguisticWindowId': 'fbahdheji',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='create',
            model=self._model,
            arguments={
                'data': data,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def create_many(
        self,
        data: List[types.TrainingLinguisticSignalCreateWithoutRelationsInput],
        *,
        skip_duplicates: Optional[bool] = None,
    ) -> int:
        """Create multiple TrainingLinguisticSignal records at once.

        This function is *not* available when using SQLite.

        Parameters
        ----------
        data
            List of TrainingLinguisticSignal record data
        skip_duplicates
            Boolean flag for ignoring unique constraint errors

        Returns
        -------
        int
            The total number of records created

        Raises
        ------
        prisma.errors.UnsupportedDatabaseError
            Attempting to query when using SQLite
        prisma.errors.UniqueViolationError
            A unique constraint check has failed, these can be ignored with the `skip_duplicates` argument
        prisma.errors.MissingRequiredValueError
            Value is required but was not found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        total = await TrainingLinguisticSignal.prisma().create_many(
            data=[
                {
                    # data to create a TrainingLinguisticSignal record
                    'trainingJobId': 'cbbheiicgh',
                    'linguisticWindowId': 'beabjeejdg',
                },
                {
                    # data to create a TrainingLinguisticSignal record
                    'trainingJobId': 'bcjhgahffd',
                    'linguisticWindowId': 'fbjeiiffa',
                },
            ],
            skip_duplicates=True,
        )
        ```
        """
        if skip_duplicates and self._client._active_provider in CREATE_MANY_SKIP_DUPLICATES_UNSUPPORTED:
            raise errors.UnsupportedDatabaseError(self._client._active_provider, 'create_many_skip_duplicates')

        resp = await self._client._execute(
            method='create_many',
            model=self._model,
            arguments={
                'data': data,
                'skipDuplicates': skip_duplicates,
            },
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    async def delete(
        self,
        where: types.TrainingLinguisticSignalWhereUniqueInput,
        include: Optional[types.TrainingLinguisticSignalInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Delete a single TrainingLinguisticSignal record.

        Parameters
        ----------
        where
            TrainingLinguisticSignal filter to select the record to be deleted, must be unique
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The deleted TrainingLinguisticSignal record
        None
            Could not find a record to delete

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().delete(
            where={
                'id': 'jhgidcgbf',
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='delete',
                model=self._model,
                arguments={
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def find_unique(
        self,
        where: types.TrainingLinguisticSignalWhereUniqueInput,
        include: Optional[types.TrainingLinguisticSignalInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Find a unique TrainingLinguisticSignal record.

        Parameters
        ----------
        where
            TrainingLinguisticSignal filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The found TrainingLinguisticSignal record
        None
            No record matching the given input could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().find_unique(
            where={
                'id': 'bgjgecfejc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None
        return model_parse(self._model, result)

    async def find_unique_or_raise(
        self,
        where: types.TrainingLinguisticSignalWhereUniqueInput,
        include: Optional[types.TrainingLinguisticSignalInclude] = None
    ) -> _PrismaModelT:
        """Find a unique TrainingLinguisticSignal record. Raises `RecordNotFoundError` if no record is found.

        Parameters
        ----------
        where
            TrainingLinguisticSignal filter to find the record, must be unique
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The found TrainingLinguisticSignal record

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().find_unique_or_raise(
            where={
                'id': 'bgjcgchib',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_unique_or_raise',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def find_many(
        self,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None,
        cursor: Optional[types.TrainingLinguisticSignalWhereUniqueInput] = None,
        include: Optional[types.TrainingLinguisticSignalInclude] = None,
        order: Optional[Union[types.TrainingLinguisticSignalOrderByInput, List[types.TrainingLinguisticSignalOrderByInput]]] = None,
        distinct: Optional[List[types.TrainingLinguisticSignalScalarFieldKeys]] = None,
    ) -> List[_PrismaModelT]:
        """Find multiple TrainingLinguisticSignal records.

        An empty list is returned if no records could be found.

        Parameters
        ----------
        take
            Limit the maximum number of TrainingLinguisticSignal records returned
        skip
            Ignore the first N results
        where
            TrainingLinguisticSignal filter to select records
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model
        order
            Order the returned TrainingLinguisticSignal records by any field
        distinct
            Filter TrainingLinguisticSignal records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        List[prisma.models.TrainingLinguisticSignal]
            The list of all TrainingLinguisticSignal records that could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the first 10 TrainingLinguisticSignal records
        traininglinguisticsignals = await TrainingLinguisticSignal.prisma().find_many(take=10)

        # find the first 5 TrainingLinguisticSignal records ordered by the linguisticWindowId field
        traininglinguisticsignals = await TrainingLinguisticSignal.prisma().find_many(
            take=5,
            order={
                'linguisticWindowId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_many',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return [model_parse(self._model, r) for r in resp['data']['result']]

    async def find_first(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None,
        cursor: Optional[types.TrainingLinguisticSignalWhereUniqueInput] = None,
        include: Optional[types.TrainingLinguisticSignalInclude] = None,
        order: Optional[Union[types.TrainingLinguisticSignalOrderByInput, List[types.TrainingLinguisticSignalOrderByInput]]] = None,
        distinct: Optional[List[types.TrainingLinguisticSignalScalarFieldKeys]] = None,
    ) -> Optional[_PrismaModelT]:
        """Find a single TrainingLinguisticSignal record.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TrainingLinguisticSignal filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model
        order
            Order the returned TrainingLinguisticSignal records by any field
        distinct
            Filter TrainingLinguisticSignal records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The first TrainingLinguisticSignal record found, matching the given arguments
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TrainingLinguisticSignal record ordered by the driftWindowId field
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().find_first(
            skip=1,
            order={
                'driftWindowId': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        result = resp['data']['result']
        if result is None:
            return None

        return model_parse(self._model, result)

    async def find_first_or_raise(
        self,
        skip: Optional[int] = None,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None,
        cursor: Optional[types.TrainingLinguisticSignalWhereUniqueInput] = None,
        include: Optional[types.TrainingLinguisticSignalInclude] = None,
        order: Optional[Union[types.TrainingLinguisticSignalOrderByInput, List[types.TrainingLinguisticSignalOrderByInput]]] = None,
        distinct: Optional[List[types.TrainingLinguisticSignalScalarFieldKeys]] = None,
    ) -> _PrismaModelT:
        """Find a single TrainingLinguisticSignal record. Raises `RecordNotFoundError` if no record was found.

        Parameters
        ----------
        skip
            Ignore the first N records
        where
            TrainingLinguisticSignal filter to select the record
        cursor
            Specifies the position in the list to start returning results from, (typically an ID field)
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model
        order
            Order the returned TrainingLinguisticSignal records by any field
        distinct
            Filter TrainingLinguisticSignal records by either a single distinct field or distinct combinations of fields

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The first TrainingLinguisticSignal record found, matching the given arguments

        Raises
        ------
        prisma.errors.RecordNotFoundError
            No record was found
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # find the second TrainingLinguisticSignal record ordered by the signalWeight field
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().find_first_or_raise(
            skip=1,
            order={
                'signalWeight': 'desc',
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='find_first_or_raise',
            model=self._model,
            arguments={
                'skip': skip,
                'where': where,
                'order_by': order,
                'cursor': cursor,
                'include': include,
                'distinct': distinct,
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update(
        self,
        data: types.TrainingLinguisticSignalUpdateInput,
        where: types.TrainingLinguisticSignalWhereUniqueInput,
        include: Optional[types.TrainingLinguisticSignalInclude] = None
    ) -> Optional[_PrismaModelT]:
        """Update a single TrainingLinguisticSignal record.

        Parameters
        ----------
        data
            TrainingLinguisticSignal record data specifying what to update
        where
            TrainingLinguisticSignal filter to select the unique record to create / update
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The updated TrainingLinguisticSignal record
        None
            No record could be found

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().update(
            where={
                'id': 'bacdaibgfa',
            },
            data={
                # data to update the TrainingLinguisticSignal record to
            },
        )
        ```
        """
        try:
            resp = await self._client._execute(
                method='update',
                model=self._model,
                arguments={
                    'data': data,
                    'where': where,
                    'include': include,
                },
            )
        except errors.RecordNotFoundError:
            return None

        return model_parse(self._model, resp['data']['result'])

    async def upsert(
        self,
        where: types.TrainingLinguisticSignalWhereUniqueInput,
        data: types.TrainingLinguisticSignalUpsertInput,
        include: Optional[types.TrainingLinguisticSignalInclude] = None,
    ) -> _PrismaModelT:
        """Updates an existing record or create a new one

        Parameters
        ----------
        where
            TrainingLinguisticSignal filter to select the unique record to create / update
        data
            Data specifying what fields to set on create and update
        include
            Specifies which relations should be loaded on the returned TrainingLinguisticSignal model

        Returns
        -------
        prisma.models.TrainingLinguisticSignal
            The created or updated TrainingLinguisticSignal record

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python
        prisma.errors.MissingRequiredValueError
            Value is required but was not found

        Example
        -------
        ```py
        traininglinguisticsignal = await TrainingLinguisticSignal.prisma().upsert(
            where={
                'id': 'dchgibach',
            },
            data={
                'create': {
                    'id': 'dchgibach',
                    'trainingJobId': 'bcjhgahffd',
                    'linguisticWindowId': 'fbjeiiffa',
                },
                'update': {
                    'trainingJobId': 'bcjhgahffd',
                    'linguisticWindowId': 'fbjeiiffa',
                },
            },
        )
        ```
        """
        resp = await self._client._execute(
            method='upsert',
            model=self._model,
            arguments={
                'where': where,
                'include': include,
                'create': data.get('create'),
                'update': data.get('update'),
            },
        )
        return model_parse(self._model, resp['data']['result'])

    async def update_many(
        self,
        data: types.TrainingLinguisticSignalUpdateManyMutationInput,
        where: types.TrainingLinguisticSignalWhereInput,
    ) -> int:
        """Update multiple TrainingLinguisticSignal records

        Parameters
        ----------
        data
            TrainingLinguisticSignal data to update the selected TrainingLinguisticSignal records to
        where
            Filter to select the TrainingLinguisticSignal records to update

        Returns
        -------
        int
            The total number of TrainingLinguisticSignal records that were updated

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # update all TrainingLinguisticSignal records
        total = await TrainingLinguisticSignal.prisma().update_many(
            data={
                'createdAt': datetime.datetime.utcnow()
            },
            where={}
        )
        ```
        """
        resp = await self._client._execute(
            method='update_many',
            model=self._model,
            arguments={'data': data, 'where': where,},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    @overload
    async def count(
        self,
        select: None = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None,
        cursor: Optional[types.TrainingLinguisticSignalWhereUniqueInput] = None,
    ) -> int:
        """Count the number of TrainingLinguisticSignal records present in the database

        Parameters
        ----------
        select
            Select the TrainingLinguisticSignal fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TrainingLinguisticSignal filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TrainingLinguisticSignalCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await TrainingLinguisticSignal.prisma().count()

        # results: prisma.types.TrainingLinguisticSignalCountAggregateOutput
        results = await TrainingLinguisticSignal.prisma().count(
            select={
                '_all': True,
                'id': True,
            },
        )
        ```
        """


    @overload
    async def count(
        self,
        select: types.TrainingLinguisticSignalCountAggregateInput,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None,
        cursor: Optional[types.TrainingLinguisticSignalWhereUniqueInput] = None,
    ) -> types.TrainingLinguisticSignalCountAggregateOutput:
        ...

    async def count(
        self,
        select: Optional[types.TrainingLinguisticSignalCountAggregateInput] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None,
        cursor: Optional[types.TrainingLinguisticSignalWhereUniqueInput] = None,
    ) -> Union[int, types.TrainingLinguisticSignalCountAggregateOutput]:
        """Count the number of TrainingLinguisticSignal records present in the database

        Parameters
        ----------
        select
            Select the TrainingLinguisticSignal fields to be counted
        take
            Limit the maximum result
        skip
            Ignore the first N records
        where
            TrainingLinguisticSignal filter to find records
        cursor
            Specifies the position in the list to start counting results from, (typically an ID field)
        order
            This parameter is deprecated and will be removed in a future release

        Returns
        -------
        int
            The total number of records found, returned if `select` is not given

        prisma.types.TrainingLinguisticSignalCountAggregateOutput
            Data returned when `select` is used, the fields present in this dictionary will
            match the fields passed in the `select` argument

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # total: int
        total = await TrainingLinguisticSignal.prisma().count()

        # results: prisma.types.TrainingLinguisticSignalCountAggregateOutput
        results = await TrainingLinguisticSignal.prisma().count(
            select={
                '_all': True,
                'trainingJobId': True,
            },
        )
        ```
        """

        # TODO: this selection building should be moved to the QueryBuilder
        #
        # note the distinction between checking for `not select` here and `select is None`
        # later is to handle the case that the given select dictionary is empty, this
        # is a limitation of our types.
        if not select:
            root_selection = ['_count { _all }']
        else:

            root_selection = [
                '_count {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))
            ]

        resp = await self._client._execute(
            method='count',
            model=self._model,
            arguments={
                'take': take,
                'skip': skip,
                'where': where,
                'cursor': cursor,
            },
            root_selection=root_selection,
        )

        if select is None:
            return cast(int, resp['data']['result']['_count']['_all'])
        else:
            return cast(types.TrainingLinguisticSignalCountAggregateOutput, resp['data']['result']['_count'])

    async def delete_many(
        self,
        where: Optional[types.TrainingLinguisticSignalWhereInput] = None
    ) -> int:
        """Delete multiple TrainingLinguisticSignal records.

        Parameters
        ----------
        where
            Optional TrainingLinguisticSignal filter to find the records to be deleted

        Returns
        -------
        int
            The total number of TrainingLinguisticSignal records that were deleted

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # delete all TrainingLinguisticSignal records
        total = await TrainingLinguisticSignal.prisma().delete_many()
        ```
        """
        resp = await self._client._execute(
            method='delete_many',
            model=self._model,
            arguments={'where': where},
            root_selection=['count'],
        )
        return int(resp['data']['result']['count'])

    # TODO: make this easier to work with safely, currently output fields are typed as
    #       not required, we should refactor the return type
    # TODO: consider returning a Dict where the keys are a Tuple of the `by` selection
    # TODO: statically type that the order argument is required when take or skip are present
    async def group_by(
        self,
        by: List['types.TrainingLinguisticSignalScalarFieldKeys'],
        *,
        where: Optional['types.TrainingLinguisticSignalWhereInput'] = None,
        take: Optional[int] = None,
        skip: Optional[int] = None,
        avg: Optional['types.TrainingLinguisticSignalAvgAggregateInput'] = None,
        sum: Optional['types.TrainingLinguisticSignalSumAggregateInput'] = None,
        min: Optional['types.TrainingLinguisticSignalMinAggregateInput'] = None,
        max: Optional['types.TrainingLinguisticSignalMaxAggregateInput'] = None,
        having: Optional['types.TrainingLinguisticSignalScalarWhereWithAggregatesInput'] = None,
        count: Optional[Union[bool, 'types.TrainingLinguisticSignalCountAggregateInput']] = None,
        order: Optional[Union[Mapping['types.TrainingLinguisticSignalScalarFieldKeys', 'types.SortOrder'], List[Mapping['types.TrainingLinguisticSignalScalarFieldKeys', 'types.SortOrder']]]] = None,
    ) -> List['types.TrainingLinguisticSignalGroupByOutput']:
        """Group TrainingLinguisticSignal records by one or more field values and perform aggregations
        each group such as finding the average.

        Parameters
        ----------
        by
            List of scalar TrainingLinguisticSignal fields to group records by
        where
            TrainingLinguisticSignal filter to select records
        take
            Limit the maximum number of TrainingLinguisticSignal records returned
        skip
            Ignore the first N records
        avg
            Adds the average of all values of the specified fields to the `_avg` field
            in the returned data.
        sum
            Adds the sum of all values of the specified fields to the `_sum` field
            in the returned data.
        min
            Adds the smallest available value for the specified fields to the `_min` field
            in the returned data.
        max
            Adds the largest available value for the specified fields to the `_max` field
            in the returned data.
        count
            Adds a count of non-fields to the `_count` field in the returned data.
        having
            Allows you to filter groups by an aggregate value - for example only return
            groups having an average age less than 50.
        order
            Lets you order the returned list by any property that is also present in `by`.
            Only **one** field is allowed at a time.

        Returns
        -------
        List[prisma.types.TrainingLinguisticSignalGroupByOutput]
            A list of dictionaries representing the TrainingLinguisticSignal record,
            this will also have additional fields present if aggregation arguments
            are used (see the above parameters)

        Raises
        ------
        prisma.errors.PrismaError
            Catch all for every exception raised by Prisma Client Python

        Example
        -------
        ```py
        # group TrainingLinguisticSignal records by linguisticWindowId values
        # and count how many records are in each group
        results = await TrainingLinguisticSignal.prisma().group_by(
            ['linguisticWindowId'],
            count=True,
        )
        ```
        """
        if order is None:
            if take is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'take\' is present')

            if skip is not None:
                raise TypeError('Missing argument: \'order\' which is required when \'skip\' is present')

        root_selection: List[str] = [*by]
        if avg is not None:
            root_selection.append(_select_fields('_avg', avg))

        if min is not None:
            root_selection.append(_select_fields('_min', min))

        if sum is not None:
            root_selection.append(_select_fields('_sum', sum))

        if max is not None:
            root_selection.append(_select_fields('_max', max))

        if count is not None:
            if count is True:
                root_selection.append('_count { _all }')
            elif isinstance(count, dict):
                root_selection.append(_select_fields('_count', count))

        resp = await self._client._execute(
            method='group_by',
            model=self._model,
            arguments={
                'by': by,
                'take': take,
                'skip': skip,
                'where': where,
                'having': having,
                'orderBy': order,
            },
            root_selection=root_selection,
        )
        return resp['data']['result']  # type: ignore[no-any-return]



def _select_fields(root: str, select: Mapping[str, Any]) -> str:
    """Helper to build a GraphQL selection string

    This is a work around until field selection is added to the query builder.
    """

    return root + ' {{ {0} }}'.format(' '.join(k for k, v in select.items() if v is True))


from . import models